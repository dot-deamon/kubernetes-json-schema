{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "$ref": "#/definitions/MemoryAllocatorManager",
    "definitions": {
        "MemoryAllocatorManager": {
            "properties": {
                "bytes_to_release": {
                    "type": "string",
                    "description": "Configures tcmalloc to perform background release of free memory in amount of bytes per ``memory_release_interval`` interval.\n If equals to ``0``, no memory release will occur. Defaults to ``0``."
                },
                "memory_release_interval": {
                    "pattern": "^([0-9]+\\.?[0-9]*|\\.[0-9]+)s$",
                    "type": "string",
                    "description": "Interval in milliseconds for memory releasing. If specified, during every\n interval Envoy will try to release ``bytes_to_release`` of free memory back to operating system for reuse.\n Defaults to 1000 milliseconds.",
                    "format": "regex"
                }
            },
            "additionalProperties": true,
            "type": "object"
        }
    }
}